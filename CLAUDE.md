# Vibe Coding 工程化开发规范

> 本文件是 AI 编程助手的行为规范。请在每次任务开始时阅读本文件，并严格遵守以下规则。
> 核心原则：**用工程流程驾驭 AI，而不是让 AI 自由发挥。**

---

## 一、项目文档体系（必须存在以下文件）

在写任何代码之前，项目根目录必须存在以下三份文档。如果不存在，先协助用户创建：

1. **`PRD.md`** — 产品需求文档
   - 包含：功能列表、每个功能的验收标准（成功跳转哪里、失败如何处理）、用户故事
   - 验收标准由人类定义，AI 不得自行编造

2. **`ARCH.md`** — 轻量架构文档
   - 包含：技术栈选择及理由、目录结构、模块划分、数据流、API 设计
   - 这是"草案"，允许迭代调整，但每次调整必须同步更新本文件

3. **`PROJECT_STATUS.md`** — 项目当前状态
   - 包含：已完成功能、当前进行中任务、已知问题、下一步计划
   - **每次任务结束后，AI 必须主动更新此文件**

如果用户还没有这些文件，引导用户通过以下流程创建：
- 先聊需求 → 整理成 PRD.md → 用户确认验收标准
- 讨论技术选型 → 生成 ARCH.md
- 初始化 PROJECT_STATUS.md

---

## 二、开发规范（reference/ 目录）

项目应包含 `reference/` 目录，存放：
- 代码风格规范（命名规则、文件组织）
- UI 设计参考（截图、设计稿链接）
- 第三方 API 文档摘要
- 示例代码片段

AI 在编码时必须参考 `reference/` 中的规范，确保代码风格一致。

---

## 三、开发过程中的五条铁律

### 铁律 1：小步迭代 + 端到端切片

- **每次只做一个最小的、能跑通的功能切片**
- 禁止一次性生成大量代码。每个任务应该是：写一小块 → 用户验收 → 再写下一块
- 如果用户给出一个大需求，AI 必须先将其拆解为多个小任务，列出清单，让用户确认优先级后，再逐个执行
- 每个切片必须是"端到端"的：从前端到后端到数据库，一条路径完整跑通

### 铁律 2：主动拆分模块，禁止一坨代码

- 单个文件不得超过 **300 行**（特殊情况需说明原因）
- 功能逻辑必须按模块拆分为独立文件
- 如果发现已有文件过长，先提议重构拆分，得到用户同意后再进行
- 组件、工具函数、类型定义、常量等必须分文件存放

### 铁律 3：禁止擅自做主 + 状态摘要管理

- **不得擅自修改不在当前任务范围内的代码**
- **不得擅自添加需求文档中没有的功能**
- **不得擅自更换技术方案或依赖库**
- 如果认为需要调整，必须先向用户说明理由，获得同意后再执行
- **每次任务结束时，必须更新 `PROJECT_STATUS.md`**，内容包括：
  - 本次完成了什么
  - 修改了哪些文件
  - 当前已知问题
  - 建议的下一步

### 铁律 4：死守安全底线（清单式检查）

每次涉及以下内容时，必须逐项检查：

- [ ] 所有 API 接口是否做了鉴权校验？
- [ ] 用户输入是否做了校验和转义（防 XSS / SQL 注入）？
- [ ] 敏感信息（密钥、密码）是否使用环境变量，而非硬编码？
- [ ] 文件上传是否限制了类型和大小？
- [ ] 数据库操作是否使用了参数化查询？
- [ ] 错误信息是否避免暴露系统内部细节？

如果用户没有提安全需求，AI 仍然必须主动执行以上检查。

### 铁律 5：科学应对报错

当遇到报错时，遵循以下流程：

1. **先缩小问题面**：定位是前端/后端/数据库/第三方服务的问题
2. **添加关键日志**：在可疑位置打印关键变量和分支走向
3. **两次尝试规则**：如果连续两次修复尝试都没有新增有效证据（新的日志输出、新的错误信息），必须**停下来**，向用户汇报当前排查进展，请求更多信息或换一个排查方向
4. **禁止盲目重试**：不得在没有新信息的情况下反复修改同一段代码

---

## 四、Git 规范

- 每完成一个功能切片，建议用户提交一次 commit
- Commit message 格式：`<type>: <简短描述>`
  - type: feat / fix / refactor / docs / style / test / chore
- 重大改动前，建议用户创建新分支

---

## 五、任务执行模板

每次收到开发任务时，AI 应按以下结构回复：

```
📋 任务理解
- 我理解你要做的是：[复述任务]
- 涉及的模块：[列出模块]
- 对应 PRD 中的功能点：[引用]

📝 执行计划
1. [第一步：具体动作]
2. [第二步：具体动作]
...

⚠️ 需要确认
- [任何不确定的点，先问再做]

---
[得到用户确认后，开始执行]
---

✅ 完成摘要
- 修改的文件：[列表]
- 新增的文件：[列表]
- 验收方法：[如何测试这个功能]
- PROJECT_STATUS.md 已更新
```

---

## 六、禁止行为清单

- ❌ 不看文档就开始写代码
- ❌ 一次性生成超过 100 行的代码块而不分步
- ❌ 擅自添加 PRD 中没有的功能
- ❌ 擅自更换技术栈或依赖
- ❌ 在报错时不分析就反复重试
- ❌ 忽略安全检查清单
- ❌ 任务结束不更新 PROJECT_STATUS.md
- ❌ 把所有代码塞进一个文件

---

## 七、从零开始的项目初始化流程

如果这是一个全新项目，按以下顺序执行：

1. **倒需求**：通过对话帮用户理清所有需求
2. **生成 PRD.md**：整理需求文档，用户定验收标准
3. **定视觉**：确认 UI 风格、页面框架、配色方案
4. **明确边界**：非功能需求（性能、并发、安全等级）
5. **选技术栈**：选择用户能验证对错的技术
6. **出架构草案**：生成 ARCH.md
7. **固化文档**：确保 PRD.md / ARCH.md / PROJECT_STATUS.md 就位
8. **建规范**：创建 reference/ 目录，写入编码规范
9. **初始化项目**：Git init、基础目录结构、质量工具配置
10. **开始第一个功能切片**
